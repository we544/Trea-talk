# Sign Language AI App

Full stack app with:
- React Native (Expo) frontend
- Flask + MediaPipe backend
- TensorFlow model placeholder

## Setup

### Backend
cd backend
pip install -r requirements.txt
python app.py

### Mobile
cd mobile-app
npm install
npx expo start
